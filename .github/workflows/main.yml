#
# Copyright 2024 NVIDIA
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Release

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      channel:
        description: "Which channel to build (schedule always builds all)"
        required: false
        default: "all"
        type: choice
        options:
          - all
          - beta-26.4
          - beta-26.8
          - dev-stable
      ref:
        description: "Override git ref for a single channel. Ignored when channel is all."
        required: false
        default: ""
        type: string
      tag:
        description: "Immutable prerelease override for a single channel (e.g. 26.4.2-beta.1). Ignored when channel is all."
        required: false
        default: ""
        type: string
      dry_run:
        description: "Dry run — build only, skip push"
        required: false
        default: false
        type: boolean

permissions:
  contents: read
  packages: write

jobs:
  beta-26-4:
    if: ${{ github.event_name == 'schedule' || inputs.channel == 'all' || inputs.channel == 'beta-26.4' }}
    concurrency:
      group: release-26.4.2-beta
      cancel-in-progress: false
    uses: ./.github/workflows/release-build.yml
    with:
      source_ref: ${{ inputs.channel == 'beta-26.4' && inputs.ref || 'public-release-v26.4' }}
      release_version: "26.4.2"
      release_channel: "beta"
      tag: ${{ inputs.channel == 'beta-26.4' && inputs.tag || '' }}
      dry_run: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run || false }}
    secrets: inherit

  beta-26-8:
    if: ${{ github.event_name == 'schedule' || inputs.channel == 'all' || inputs.channel == 'beta-26.8' }}
    concurrency:
      group: release-26.8.0-beta
      cancel-in-progress: false
    uses: ./.github/workflows/release-build.yml
    with:
      source_ref: ${{ inputs.channel == 'beta-26.8' && inputs.ref || 'public-release-v26.8' }}
      release_version: "26.8.0"
      release_channel: "beta"
      tag: ${{ inputs.channel == 'beta-26.8' && inputs.tag || '' }}
      dry_run: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run || false }}
    secrets: inherit

  dev-stable:
    if: ${{ github.event_name == 'schedule' || inputs.channel == 'all' || inputs.channel == 'dev-stable' }}
    concurrency:
      group: release-0.0.0-dev-stable
      cancel-in-progress: false
    uses: ./.github/workflows/release-build.yml
    with:
      source_ref: ${{ inputs.channel == 'dev-stable' && inputs.ref || 'public-main' }}
      release_version: "0.0.0"
      release_channel: "dev-stable"
      floating_tag: "dev-stable"
      tag: ${{ inputs.channel == 'dev-stable' && inputs.tag || '' }}
      dry_run: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run || false }}
    secrets: inherit
